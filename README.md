OK so here it is.  Dealing with repos kind of sucks for most devs.  This app will hopefully make it easer.

TODO:
- Write REST interface + JSON format
- CRUD RPMs
- CRUD repos
- /runCreateRepo endpoint
